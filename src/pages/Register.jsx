import React from 'react';
import RegistrationForm from '../components/RegistrationForm';

const Register = () => {    
    return (
      <div className="registration-page">
        <RegistrationForm />
      </div>
    );
  };

export default Register;  // Ensure you have this line
